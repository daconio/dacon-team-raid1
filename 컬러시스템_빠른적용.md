# 🎨 컬러 시스템 빠른 적용 가이드

**소요 시간**: 10분  
**난이도**: ⭐ 쉬움

---

## 📋 적용할 색상

```css
✅ 기본색 (Primary): #fcd34d (노란색)
✅ 강조색 (Secondary): #3b82f6 (파란색)
✅ 테두리: #000000 (검은색)
✅ 배경: #f8f9fa (밝은 회색)
```

---

## 🚀 3단계로 빠르게 적용하기

### Step 1: Tailwind 설정 파일 교체 (2분)

#### `client/tailwind.config.js` 파일을 열고 전체 내용을 아래로 교체:

```javascript
/** @type {import('tailwindcss').Config} */
export default {
  content: [
    "./index.html",
    "./src/**/*.{js,ts,jsx,tsx}",
  ],
  theme: {
    extend: {
      colors: {
        primary: {
          50: '#fffbeb',
          100: '#fef3c7',
          200: '#fde68a',
          300: '#fcd34d',
          400: '#fbbf24',
          500: '#f59e0b',
          600: '#d97706',
          700: '#b45309',
          800: '#92400e',
          900: '#78350f',
        },
        secondary: {
          50: '#eff6ff',
          100: '#dbeafe',
          200: '#bfdbfe',
          300: '#93c5fd',
          400: '#60a5fa',
          500: '#3b82f6',
          600: '#2563eb',
          700: '#1d4ed8',
          800: '#1e40af',
          900: '#1e3a8a',
        },
        background: {
          DEFAULT: '#f8f9fa',
          secondary: '#e9ecef',
          card: '#ffffff',
        },
        text: {
          primary: '#212529',
          secondary: '#6c757d',
          muted: '#adb5bd',
        },
        border: {
          DEFAULT: '#000000',
          light: '#dee2e6',
        },
        success: {
          DEFAULT: '#10b981',
          light: '#d1fae5',
          dark: '#065f46',
        },
        warning: {
          DEFAULT: '#f59e0b',
          light: '#fef3c7',
          dark: '#92400e',
        },
        error: {
          DEFAULT: '#ef4444',
          light: '#fee2e2',
          dark: '#991b1b',
        },
      },
      boxShadow: {
        'DEFAULT': '0 1px 3px 0 rgba(0, 0, 0, 0.1)',
        'md': '0 4px 6px -1px rgba(0, 0, 0, 0.1)',
        'lg': '0 10px 15px -3px rgba(0, 0, 0, 0.1)',
        'xl': '0 20px 25px -5px rgba(0, 0, 0, 0.1)',
        'strong': '0 10px 25px rgba(0, 0, 0, 0.25)',
      },
    },
  },
  plugins: [],
}
```

---

### Step 2: 전역 스타일 교체 (3분)

#### `client/src/index.css` 파일을 열고 전체 내용을 아래로 교체:

```css
@tailwind base;
@tailwind components;
@tailwind utilities;

@layer base {
  body {
    @apply bg-background text-text-primary;
  }
}

@layer components {
  .btn-primary {
    @apply bg-primary-300 hover:bg-primary-400 active:bg-primary-500 
           text-text-primary font-semibold py-2 px-4 rounded-lg 
           transition-all duration-200 border border-border
           shadow hover:shadow-md;
  }
  
  .btn-secondary {
    @apply bg-secondary-500 hover:bg-secondary-600 active:bg-secondary-700 
           text-white font-semibold py-2 px-4 rounded-lg 
           transition-all duration-200 border border-border
           shadow hover:shadow-md;
  }
  
  .btn-outline {
    @apply bg-transparent hover:bg-background-secondary 
           text-text-primary font-semibold py-2 px-4 rounded-lg 
           transition-all duration-200 border-2 border-border
           hover:shadow;
  }

  .btn-ghost {
    @apply bg-transparent hover:bg-background-secondary 
           text-text-primary font-semibold py-2 px-4 rounded-lg 
           transition-all duration-200;
  }
  
  .card {
    @apply bg-background-card rounded-xl p-6 
           border-2 border-border shadow-md;
  }

  .card-hover {
    @apply bg-background-card rounded-xl p-6 
           border-2 border-border shadow-md
           transition-all duration-200
           hover:shadow-lg hover:border-secondary-500;
  }
  
  .input {
    @apply w-full px-4 py-2 
           bg-background-card border-2 border-border rounded-lg 
           text-text-primary placeholder-text-muted
           focus:outline-none focus:border-secondary-500 focus:ring-2 focus:ring-secondary-500/20
           transition-all duration-200;
  }
  
  .select {
    @apply w-full px-4 py-2 
           bg-background-card border-2 border-border rounded-lg 
           text-text-primary
           focus:outline-none focus:border-secondary-500 focus:ring-2 focus:ring-secondary-500/20
           transition-all duration-200;
  }
  
  .textarea {
    @apply w-full px-4 py-2 
           bg-background-card border-2 border-border rounded-lg 
           text-text-primary placeholder-text-muted
           focus:outline-none focus:border-secondary-500 focus:ring-2 focus:ring-secondary-500/20
           transition-all duration-200
           resize-vertical;
  }

  .badge {
    @apply inline-block px-3 py-1 text-sm font-semibold rounded-full;
  }

  .badge-primary {
    @apply bg-primary-300 text-text-primary border border-border;
  }

  .badge-secondary {
    @apply bg-secondary-500 text-white;
  }

  .badge-success {
    @apply bg-success text-white;
  }

  .badge-warning {
    @apply bg-warning text-white;
  }

  .badge-error {
    @apply bg-error text-white;
  }
}

@layer utilities {
  .gradient-primary {
    background: linear-gradient(135deg, #fcd34d 0%, #f59e0b 100%);
  }

  .gradient-secondary {
    background: linear-gradient(135deg, #3b82f6 0%, #1d4ed8 100%);
  }
}
```

---

### Step 3: 개발 서버 재시작 (1분)

```bash
# 기존 서버 중지 (Ctrl + C)

# 다시 시작
npm run dev
```

---

## ✅ 적용 확인 체크리스트

브라우저에서 다음을 확인하세요:

### 기본 확인
- [ ] 배경색이 밝은 회색 (#f8f9fa)으로 변경됨
- [ ] 카드가 흰색 배경에 검은 테두리로 표시됨
- [ ] 버튼이 노란색/파란색으로 표시됨

### 인터랙션 확인
- [ ] 버튼에 마우스를 올렸을 때 색이 진해짐
- [ ] 카드에 마우스를 올렸을 때 테두리가 파란색으로 변경됨
- [ ] 입력 필드 포커스 시 파란색 테두리로 변경됨

### 페이지별 확인
- [ ] 홈페이지 - 원정대 카드 색상
- [ ] 원정대 상세 - 버튼 및 뱃지 색상
- [ ] 프로필 페이지 - 정보 카드 색상
- [ ] 원정대 생성 - 입력 필드 색상

---

## 🎨 주요 클래스 사용법

### 버튼
```tsx
<button className="btn-primary">주요 액션</button>
<button className="btn-secondary">보조 액션</button>
<button className="btn-outline">덜 중요한 액션</button>
<button className="btn-ghost">최소 강조</button>
```

### 카드
```tsx
<div className="card">기본 카드</div>
<div className="card-hover">호버 효과가 있는 카드</div>
```

### 입력 필드
```tsx
<input className="input" placeholder="입력하세요..." />
<select className="select">...</select>
<textarea className="textarea" placeholder="내용을 입력하세요..." />
```

### 뱃지
```tsx
<span className="badge badge-primary">Primary</span>
<span className="badge badge-secondary">Secondary</span>
<span className="badge badge-success">Success</span>
<span className="badge badge-warning">Warning</span>
<span className="badge badge-error">Error</span>
```

---

## 🔄 기존 다크 테마로 되돌리기

만약 다크 테마로 되돌리고 싶다면:

### 1. Tailwind 설정 되돌리기
```javascript
// neutral-900, neutral-800 등 다크 색상으로 변경
colors: {
  primary: {
    500: '#0ea5e9',
    600: '#0284c7',
  },
  neutral: {
    900: '#171717',
    800: '#262626',
  }
}
```

### 2. index.css 되돌리기
```css
body {
  @apply bg-neutral-900 text-white;
}
```

---

## 💡 팁

### Tailwind IntelliSense 확장 프로그램
VS Code를 사용한다면 `Tailwind CSS IntelliSense` 확장 프로그램을 설치하면 자동완성이 됩니다!

### 색상 확인 도구
브라우저 개발자 도구 (F12)에서 요소를 검사하면 실제 적용된 색상을 확인할 수 있습니다.

---

## 🐛 문제 해결

### 색상이 적용되지 않아요
```bash
# Tailwind 캐시 삭제
rm -rf node_modules/.cache
npm run dev
```

### 특정 컴포넌트만 색상이 안 바뀌어요
해당 컴포넌트 파일에서 하드코딩된 색상 클래스를 찾아 변경하세요.

예시:
```tsx
// 변경 전
<div className="bg-neutral-800 text-white">

// 변경 후
<div className="bg-background-card text-text-primary">
```

---

## 📚 더 자세한 정보

전체 컬러 시스템 가이드: [DACON_RAID_컬러시스템_가이드.md](./DACON_RAID_컬러시스템_가이드.md)

---

**적용 완료!** 🎉

이제 새로운 밝은 테마로 DACON: RAID를 즐기세요!
